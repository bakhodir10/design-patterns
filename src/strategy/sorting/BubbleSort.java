package strategy.sorting;

public class BubbleSort implements Sorting {

    // it should sort data by using quick sort
    @Override
    public int[] sort(int[] arr) {
        System.out.println("array is being sorted by using bubble sort");
        return arr;
    }
}