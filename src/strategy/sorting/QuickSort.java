package strategy.sorting;

public class QuickSort implements Sorting {

    // it should sort data by using quick sort
    @Override
    public int[] sort(int[] arr) {
        System.out.println("array is being sorted by using quick sort");
        return arr;
    }
}
